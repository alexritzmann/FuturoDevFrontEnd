const numero = prompt('Digite o número para obter resultados:');

const raizQuadrada = Math.sqrt(Number(numero));
console.log(raizQuadrada);

const quadradoNumero = Math.pow(Number(numero), 2);
console.log(quadradoNumero);

const divisaoPorTres = Number(numero) / 3;
console.log(divisaoPorTres.toFixed(3));

