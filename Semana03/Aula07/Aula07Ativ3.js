const nomeCompleto = prompt('Qual seu nome completo?');


console.log('Tamanho do nome completo:', nomeCompleto.length);
console.log('Primeira letra do nome completo:', nomeCompleto[0]);
console.log('Ultima letra do nome completo:', nomeCompleto[nomeCompleto.length - 1]);

console.log('String recebida em maiusculo:', nomeCompleto.toUpperCase());
console.log('String recebida em minusculo:', nomeCompleto.toLowerCase());

